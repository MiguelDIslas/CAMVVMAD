export interface ArrayItem {
  /**
   * Identificador del elemento
   */
  id: string;

  /**
   * Nombre del elemento
   */
  name: string;

  /**
   * Indica si el elemento está seleccionado
   */
  checked?: boolean;
}
