import {ThemeArgument} from 'styled-components/native';
import {colors} from './Colors';
import {spacing} from './Spacing';
import {fontSize} from './FontSize';

const myTheme: ThemeArgument = {
  colors,
  spacing,
  fontSize,
};

export {myTheme};
